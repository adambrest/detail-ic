{
  "name": "smart-detailer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8080",
    "test": "node --test tests/*.test.js",
    "test:browser": "node tests/browser.cjs && node tests/recovery.cjs",
    "format": "prettier --write app.js core.js profiles.js styles.css index.html tests/*.js tests/*.cjs"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "prettier": "^3.6.2"
  }
}
