const http=require('node:http');
const fs=require('node:fs/promises');
const path=require('node:path');
// Browsers refuse some low ports (e.g. 1723), so pick from a high range instead of port 0.
const listen=(server)=>new Promise((resolve,reject)=>{const tryPort=(left)=>{const port=40000+Math.floor(Math.random()*10000);server.once('error',(e)=>e.code==='EADDRINUSE'&&left?tryPort(left-1):reject(e));server.listen(port,'127.0.0.1',()=>resolve(port));};tryPort(20);});
exports.startServer=async()=>{const root=path.resolve(__dirname,'..');const server=http.createServer(async(req,res)=>{try{const requested=new URL(req.url,'http://localhost').pathname;const file=path.resolve(root,'.'+(requested==='/'?'/index.html':decodeURIComponent(requested)));if(!file.startsWith(root+path.sep)){res.writeHead(403);res.end();return;}const bytes=await fs.readFile(file);res.setHeader('Content-Type',({'.html':'text/html','.js':'application/javascript','.css':'text/css','.svg':'image/svg+xml','.png':'image/png','.webmanifest':'application/manifest+json'})[path.extname(file)]||'application/octet-stream');res.end(bytes);}catch{res.writeHead(404);res.end();}});const port=await listen(server);return {url:`http://127.0.0.1:${port}`,stop:()=>new Promise(resolve=>{server.close(resolve);server.closeAllConnections();})};};
